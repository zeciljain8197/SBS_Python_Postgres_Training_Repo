# Perform operations with all the Arithmetic Operators

(a, b) = map(int, input().split())
add = a + b
sub = a - b
mul = a * b
div = a / b
mod = a % b
flrdiv = a // b
exp = pow(a, b)

print(add, '\n', sub, '\n', mul, '\n', div, '\n', mod, '\n', flrdiv, '\n', exp)
